# Security policy

Do not open a public issue for a suspected vulnerability. Report it privately through GitHub's security advisory interface for `rosh2525/PortfolioTracker`.

Production deployments must use strong unique database, Django, and administrator credentials; HTTPS; restricted hosts and origins; and regularly updated images. Never commit `.env` files or access tokens.
